{
  "name": "aws-copy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/aws-copy/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project aws-copy --web",
  "targets": {
        "serve": {
            "executor": "@nx/vite:dev-server",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "aws-copy:build"
            },
            "configurations": {}
        }
    }
}
